@page "/consultar"
@model ClienteWeb.Pages.Client_SimarUser.SimarUser.ConsultSimarUserModel
@{
    ViewData["Title"] = "Consulta de Usuarios SIMAR";
}

<div class="container mt-4">
    <div class="card shadow">
        <div class="card-header bg-dark text-white d-flex justify-content-between align-items-center">
            <h4 class="mb-0">Usuarios SIMAR</h4>
            <a href="/registro" class="btn btn-sm btn-outline-light"> + Nuevo Registro</a>
        </div>

        <div class="card-body">
            <div class="row mb-4">
                <div class="col-md-6">
                    <label class="form-label fw-bold">Buscar por Nombre o usuario:</label>
                    <input type="text" id="busquedaInput" class="form-control" placeholder="Buscar..." onkeyup="filtrarTabla()">
                </div>
                <div class="col-md-6">
                    <label class="form-label fw-bold">Filtrar por Rol:</label>
                    <select id="rolFiltro" class="form-select" onchange="filtrarTabla()">
                        <option value="">-- Todos los Roles --</option>
                        <option value="Seller">Seller</option>
                        <option value="Technical">Technical</option>
                        <option value="Manager">Manager</option>
                        <option value="Owner">Owner</option>
                        <option value="Accountant">Accountant</option>
                        <option value="Driver">Driver</option>
                    </select>
                </div>
            </div>

            <table class="table table-striped table-hover mt-3" id="tablaUsuarios">
                <thead class="table-dark">
                    <tr>
                        <th>Usuario</th>
                        <th>Nombre</th>
                        <th>Rol</th>
                        <th>CURP</th>
                        <th>RFC</th>
                        <th>Salario</th>
                        <th class="text-center">Acciones</th>
                    </tr>
                </thead>
                <tbody>
                    @*Datos de prueba (borrarlos despues del 1er sprint)*@
                    @* Ejemplo 1 *@
                    <tr>
                        <td>Admin_Simar</td>
                        <td>Luis Enrique López</td>
                        <td><span class="badge bg-primary">Owner</span></td>
                        <td><small>LOAL88053662</small></td>
                        <td><small>RFC-9988</small></td>
                        <td>$25,000</td>
                        <td class="text-center">
                            <a href="/editar/Admin_Simar" class="btn btn-warning btn-sm">
                                <i class="bi bi-pencil"></i> Editar
                            </a>
                            <button class="btn btn-danger btn-sm" onclick="confirmarBaja('Admin_Simar')">Baja</button>
                        </td>
                    </tr>
                    @* Ejemplo 2 *@
                    <tr>
                        <td>Vendedor_01</td>
                        <td>Magdalena Luna</td>
                        <td><span class="badge bg-info text-dark">Seller</span></td>
                        <td><small>GANA92013662</small></td>
                        <td><small>RFC-12345</small></td>
                        <td>$15,000</td>
                        <td class="text-center">
                            <a href="/editar/Vendedor_01" class="btn btn-warning btn-sm">Editar</a>
                            <button class="btn btn-danger btn-sm" onclick="confirmarBaja('Vendedor_01')">Baja</button>
                        </td>
                    </tr>
                    @* Ejemplo 3 *@
                    <tr>
                        <td>Chofer_01</td>
                        <td>Miguel Nava</td>
                        <td><span class="badge bg-secondary">Driver</span></td>
                        <td><small>PEJU9012345</small></td>
                        <td><small>RFC-54321</small></td>
                        <td>$12,000</td>
                        <td class="text-center">
                            <a href="/editar/Chofer_01" class="btn btn-warning btn-sm">Editar</a>
                            <button class="btn btn-danger btn-sm" onclick="confirmarBaja('Chofer_01')">Baja</button>
                        </td>
                    </tr>
                </tbody>
            </table>
        </div>
    </div>
</div>

<script>
    function filtrarTabla() {
        // 1. Obtener valores de los filtros
        const busqueda = document.getElementById('busquedaInput').value.toLowerCase();
        const rolSeleccionado = document.getElementById('rolFiltro').value.toLowerCase();

        // 2. Obtener las filas de la tabla (excepto el encabezado)
        const filas = document.getElementById('tablaUsuarios').getElementsByTagName('tbody')[0].getElementsByTagName('tr');

        // 3. Recorrer cada fila para decidir si se muestra o se oculta
        for (let i = 0; i < filas.length; i++) {
            const celdas = filas[i].getElementsByTagName('td');
            if (celdas.length > 0) {
                const username = celdas[0].textContent.toLowerCase();
                const nombre = celdas[1].textContent.toLowerCase();
                const rol = celdas[2].textContent.toLowerCase();

                // Lógica de coincidencia
                const coincideBusqueda = username.includes(busqueda) || nombre.includes(busqueda);
                const coincideRol = rolSeleccionado === "" || rol.includes(rolSeleccionado);

                if (coincideBusqueda && coincideRol) {
                    filas[i].style.display = "";
                } else {
                    filas[i].style.display = "none";
                }
            }
        }
    }

    function confirmarBaja(usuario) {
        if (confirm("¿Estás seguro de que deseas dar de baja al usuario: " + usuario + "?")) {
            alert(usuario + " ha sido desactivado del sistema.");
        }
    }
</script>
